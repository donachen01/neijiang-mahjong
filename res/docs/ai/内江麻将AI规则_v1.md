# 内江麻将 AI 规则 v1

## 1. 文档定位

本文档用于固化当前内江麻将工程内 **实际生效** 的 AI 决策规则。

适用范围：

- 3 家电脑玩家的决策链
- 本家辅助建议链
- AI 调参面板中的中文说明与默认策略

维护约束：

- 后续凡是修改 AI 策略、调参项、阶段划分、攻守切换逻辑，都必须同步更新本文档。
- 本文档以当前工程的内江麻将实现为准，不再沿用四川麻将的定缺/花猪逻辑。

当前对应核心代码：

- `res://autoload/GameState.gd`：牌局状态、前端调度、C# 请求编排
- `res://scripts/ai/NeijiangCSharpRuntime.cs`：Godot 内嵌 C# AI 桥接
- `dotnet/AI.Core/Engines/NeijiangDecisionEngine.cs`：出牌主决策
- `dotnet/AI.Core/Engines/NeijiangExpectedScoreEngine.cs`：净分期望模型
- `dotnet/AI.Core/Engines/NeijiangSelfDrawProbabilityEngine.cs`：基于牌墙后验的自摸概率
- `dotnet/AI.Core/Engines/NeijiangReactionDecisionEngine.cs`：碰 / 杠 / 胡 / 过反应决策
- `dotnet/AI.Core/Engines/NeijiangSelfActionDecisionEngine.cs`：自摸 / 暗杠 / 补杠决策
- `dotnet/AI.Core/Engines/NeijiangBeliefEngine.cs`：读牌后验
- `dotnet/AI.Core/Engines/NeijiangDangerEngine.cs`：点炮危险度
- `dotnet/AI.Core/Learning/NeijiangLearningEngine.cs`：自进化调参记录

---

## 2. 内江麻将基础前提

当前工程的内江麻将，基础约束如下：

- 只使用 **筒、条两门**
- 总牌数固定 **72 张**
- 无 **定缺**
- 无 **花猪**
- 第一轮发牌完成后，牌墙剩余 **19 张**
- 因牌墙更浅，内江节奏比四川麻将更短、更急

这 5 条前提会直接影响 AI 的：

- 巡目阶段划分
- 报叫/报杠决策
- 听牌速度权重
- 自摸率与点炮率估算
- 尾盘防守阈值

---

## 3. AI 总体目标

当前内江 AI 的目标，不是单纯“看到番就贪大”，而是尽量像熟练老玩家：

- 优先 **尽快成叫**
- 在同向听下优先 **宽叫、活张多、自摸率更高** 的路线
- 在两门浅牌墙环境下，更早进入 **防点炮 / 保查叫 / 保报叫权益**
- 有价值时可以追 `清一色`、`暗七对`、`龙七对`、`清对`、`清七对`、`青龙七对`
- 但不会为了贪大而明显拖慢听牌

当前总原则：

- **速度优先于空贪**
- **成叫优先于花活**
- **后期不点炮优先于追更大**

---

## 4. 当前决策链结构

### 4.1 出牌主链

每张候选弃牌会综合以下维度评分：

- 向听速度：越接近 `0 向听` 越优先
- 进张数量：活张越多越优先
- 听口质量：`宽叫 > 两面 > 坎张/边张 > 单吊`
- 胡牌概率：结合剩余可见牌、后验分布、尾盘风险估算
- 番型价值：保留高价值路线，但不凌驾于成叫速度
- 结构稳定：孤张、废搭、断裂结构会被减分
- 防守风险：尾盘和高压局会显著抬权
- 自摸潜力：优先保留更容易自摸到的叫口
- 内江专项：两门牌压缩、报叫约束、卡二条收益、查叫收益

最终原则：

- 先比 **向听数**
- 向听相同再比 **进张 / 听口 / 胡率**
- 仍相同再比 **番型价值 / 风险**

### 4.2 反应链

只处理：

- 胡
- 碰
- 杠
- 过

基本原则：

- 可胡时直接胡
- 碰牌必须带来 **成叫提速** 或 **明显的结构增益**
- 杠牌必须满足 **收益 > 风险**
- 牌墙浅时，`过` 的价值会比四川麻将更高

### 4.3 杠牌链

分为：

- 暗杠
- 补杠
- 明杠

当前原则：

- 暗杠只要不破坏成叫速度，优先考虑
- 补杠重点防 `抢杠胡` 与尾盘放大风险
- 明杠不是“能杠就杠”，必须看当前是否逼近成叫、是否放大后验危险度
- 报叫后只允许杠 **报杠白名单** 内、且 **不破坏原有听型** 的牌

---

## 5. 阶段划分

内江麻将起手发完牌后只剩 19 张牌墙，因此阶段划分比四川麻将更短。

当前采用的阶段认知：

- 前期：0–5 弃牌
- 中期：6–11 弃牌
- 后期：12+ 弃牌

配套理解：

- 前期：优先整理结构、尽快逼近成叫
- 中期：开始严格比较宽叫、自摸率、报叫收益
- 后期：优先级切换为 **不点炮 / 保查叫 / 保报叫权益**

---

## 6. 局势判断规则

### 6.1 两门牌桌面判断

内江麻将没有定缺格局判断，改为识别 **两门浓度与竞争压力**：

- `单门偏重`
  - 自己某一门明显更集中
  - 可以更积极评估 `清一色` 或半染路线
- `双门均衡`
  - 两门分布接近
  - 更偏向保留宽叫与进张弹性
- `高压浅墙`
  - 牌墙低、他家副露多、有人报叫
  - 优先保命、防点炮、保成叫

### 6.2 手牌类型判断

当前仍保留 4 类粗分：

- `进攻型`
  - 对子较多、连张较多、成型快
- `均衡型`
  - 有基础搭子，攻守都能转
- `防守型`
  - 成型一般，但可保守推进
- `摆烂型`
  - 孤张多、废搭多、结构散

### 6.3 对手画像判断

AI 当前读取的主要信号：

- `fast_call_count`
  - 副露频繁、明显抢速度的对手数量
- `silent_big_hand_count`
  - 长时间不碰不杠，疑似憋大牌的对手数量
- `flush_watch_count`
  - 疑似清一色的对手数量
- `pung_watch_count`
  - 疑似对对胡/将对路线的对手数量
- `top_threat_profile`
  - 当前最危险对手及其危险花色

---

## 7. 内江高手规则落地

### 7.1 快速成叫第一优先

AI 每次出牌前，会遍历所有可打牌，模拟打出后重新计算：

- 向听数
- 可听牌面
- 活张数量
- 胡牌概率

当前基本决策顺序：

1. 先选 **向听数最小**
2. 若同向听，选 **进张更多**
3. 若同进张，选 **宽叫更优**
4. 若同宽度，选 **自摸率更高 / 活张更多**
5. 若仍接近，再看 **净分期望**
6. 净分接近时，再看番型路线与风险解释

### 7.1.1 净分期望模型

当前出牌主链已经不再只使用“经验分”排序，而是为每一张候选弃牌计算：

- `预期胡牌收益`
  - 估算当前候选路线的基础番档
  - 按 `1番=1分、2番=2分、3番=4分、4番=8分、5番=16分` 折算
  - 自摸按“每家支付胡牌分 + 自摸额外 1 分”估算
- `预期点炮损失`
  - 使用对手听牌后验、胡这张后验、危险度估算对手番档
  - 按候选弃牌的点炮概率折算损失
- `查叫 / 报叫残局风险`
  - 如果候选牌导致未成叫或成叫概率低，后期按牌墙剩余、对手听牌后验估算查叫赔付风险
- `成叫价值`
  - 0 向听、宽叫、活张多、牌墙后验高，会形成正向补偿

简化表达：

`净分期望 = 预期胡牌收益 - 预期点炮损失 - 查叫风险 + 成叫价值`

落地代码：

- `dotnet/AI.Core/Engines/NeijiangExpectedScoreEngine.cs`
- `dotnet/AI.Core/Models/NeijiangExpectedScore.cs`
- `dotnet/AI.Core/Models/NeijiangCandidateDetail.cs`

候选输出字段：

- `expectedNetScore`
- `expectedWinGain`
- `expectedDealInLoss`
- `expectedDrawRiskLoss`
- `expectedReadyValue`

### 7.2 宽叫优先

当前明确执行：

- `宽叫 > 两面 > 坎张/边张 > 单吊`
- `听必现张` 优于普通听张
- `听剩张多` 优于听剩张少

### 7.3 自摸优先

两门浅牌墙下，AI 更偏好保留：

- 对手不容易先碰走的张
- 剩余数量更多的张
- 后验上更可能还在牌墙中的张

即：

- `活张多` 更优
- `牌墙后验更高` 更优
- `多面同时可自摸` 更优

当前自摸率不再使用简单线性经验公式，而是按以下近似计算：

1. 统计候选弃牌后的所有可胡 / 可进张听口
2. 对每个听口读取：
   - 当前剩余张数
   - `牌墙后验`
   - 当前牌墙剩余数量
3. 估算自己在剩余牌墙中的可摸次数：
   - 未胡玩家越少，自己可摸机会越多
   - 1 向听、2 向听会按距离听牌的远近折减
4. 使用 `1 - (1 - 单次摸中概率) ^ 可摸次数` 计算“至少摸中一次”的概率
5. 再用宽叫数量、活张数量、危险度做小幅修正

落地代码：

- `dotnet/AI.Core/Engines/NeijiangSelfDrawProbabilityEngine.cs`

这条规则的含义是：AI 不只看“胡张有几张”，还会看“这些张更可能在牌墙里，还是更可能被别人捏在手里”。

### 7.4 卡二条优先

若 `卡二条` 成立且不明显拖慢速度：

- 会提高其保留权重
- 在报叫收益评估中额外加分
- 在同分路线中可作为优先保留项

### 7.5 碰牌规则

当前碰牌改为“老手主动抢听”风格：不是乱碰，但在不拖慢成叫时会明显更积极。

优先碰的情况：

- 碰后直接成叫
- 碰后从 2 向听压到 1 向听
- 碰后不升向听，且活张不比原路线明显少
- 对子较多、七对路线不稳，碰后可减少对子冗余
- 前中盘碰后能定型抢听，首打风险未达到明显高危

明确不鼓励的情况：

- 碰后升向听
- 碰后听口变窄
- 碰后尾盘防守能力显著下降
- 多家高后验听牌且碰后仍未成叫

### 7.6 杠牌规则

当前杠牌链重点关注：

- 杠后是否仍保持成叫速度
- 是否触发抢杠胡风险
- 是否会在浅牌墙尾盘放大点炮风险
- 报叫后是否属于合法报杠白名单

本轮后杠牌权重改为更贴近内江“雨钱收益”：

- 暗杠 / 补杠 / 明杠的税收益进入 C# 评分
- 杠后不升向听、活张不差、首打风险可控时，明显提高执行倾向
- 杠后已经成叫，或杠后仍保持 0 向听，会更积极收雨钱
- 只有在杠后拖慢成叫、尾盘未叫、或后验风险明显高时才强降权

### 7.7 报叫规则

当前内江规则要求：

- 仅闲家支持开局报叫
- 发牌结束后手牌直接听牌，才允许报叫
- 报叫 +1 番
- 报叫后手牌锁定，不可换牌、不可碰牌
- 若报叫同时有合法报杠，则只允许杠报杠白名单中的牌

AI 当前在报叫决策中重点看：

- 听牌张数
- 活张总量
- 是否含 `卡二条`
- 查叫收益
- 是否保留报杠白名单

---

## 8. 概率读牌与后验

当前内江 AI 的概率层，重点服务于两类场景：

- 尾盘弃牌阈值
- 碰/杠是否值得做

核心后验包括：

- `听牌后验`
  - 某家已成叫/接近成叫的概率
- `持张后验`
  - 某张还在某家手中的概率
- `牌墙后验`
  - 某张仍在牌墙中的概率

当前使用方式：

- 尾盘若点炮风险明显高于自身胡牌收益，切防守
- 补杠/明杠若显著抬高被胡概率，则降权或放弃
- 同向听下优先保留牌墙后验更高的叫口

---

## 9. 记分与番型约束

当前内江规则以 **5 番封顶** 为上限，结算时不允许突破。

当前关键番型与分值：

- `清一色` = 3 番 = 4 分
- `暗七对` = 3 番 = 4 分
- `龙七对` = 4 番 = 8 分
- `清对` = 4 番 = 8 分
- `清七对` = 4 番 = 8 分
- `青龙七对` = 5 番 = 16 分

其他特殊名目如：

- `天和`
- `地胡`
- `海底`
- `杠上花`
- `杠上炮`
- `抢杠胡`

均应在结算页与底层记分逻辑中保持一致，并受 `5 番封顶` 约束。

---

## 10. 已移除的四川麻将逻辑

本内江工程内，以下四川麻将专属概念不应再参与内江 AI：

- 定缺优先
- 缺门牌强制清理
- 花猪判断
- 三家同缺 / 两家同缺 / 四家同缺 的格局推演
- 基于三门 108 张牌的未见张估算
- “别人缺门”带来的自摸偏置

若未来代码或文档再次出现以上概念，视为回归污染，需要同步修正。

---

## 11. 当前回归检查重点

当前内江专项回归应至少覆盖：

- 规则模式只使用 `条/筒`
- 总牌数固定 `72`
- 开局发牌完成后牌墙剩余 `19`
- 不进入定缺阶段
- 残留 `ding_que` 字段不会阻塞内江出牌
- 报叫/报杠/查叫/退税/卡二条规则正常
- AI 在两门浅牌墙下优先快速成叫

---

## 12. 测试、压测与真实评测口径

为避免把“烟测结果”误认为“真实实战能力”，当前工程统一采用以下测试分层口径。

### 12.1 C# 单点烟测

定位：只验证 C# AI 某个局部模块是否能独立计算并返回合理结果。

代表文件：

- `dotnet/AI.Core.Smoke/Program.cs`

适合验证：

- 向听 / 进张 / 自摸概率是否有基本输出
- 净分期望字段是否正常生成
- 碰、暗杠等单个反应样例是否返回预期动作

明确限制：

- 不是完整牌局
- 不代表真实胜率
- 不代表完整报叫、报杠、查叫、退税、结算链路均已走通
- 不允许仅凭烟测结果宣称 AI 已达到真实实战水平

### 12.2 Godot 回归测试

定位：验证 Godot 规则流程、状态迁移、界面桥接是否出现明显回归。

代表文件：

- `tests/NeijiangRegressionRunner.gd`

适合验证：

- 内江模式是否仍为两门 72 张
- 是否绕开定缺 / 花猪等四川麻将旧逻辑
- 报叫、报杠、查叫、退税、结算等规则是否能触发
- C# 返回结果能否被 Godot 状态机接收并执行

明确限制：

- 它是工程回归工具，不等同于玩家真实体验
- 可以跳过动画等待，但不能跳过规则状态迁移
- 若只验证局部路径，不能推导整体牌力强弱

### 12.3 AI 自动流程压测

定位：批量跑自动牌局流程，用于发现卡死、超时、状态残留、结算异常与统计趋势。

代表文件：

- `tests/ai_pressure_benchmark.gd`

当前脚本特征：

- 会实例化 `GameState.gd`
- 会将 4 家设为自动玩家
- 会尝试推进开局、出牌、反应、下一轮、结算等阶段
- 会统计 `score_changes`、`win_events`、`gang_events`、`ai_decision_metrics`
- 会输出到 `res://测试数据统计/ai压测_时间_局数_模式.json/csv`

当前必须使用的称呼：

- `自动流程压测`
- `近似牌局模拟`

当前禁止使用的称呼：

- `真实实战压测`
- `完整真人等价牌局`
- `已证明超越人类`

原因：该脚本虽然比 C# 烟测更接近完整牌局，但仍需逐项审计，确认它没有跳过关键规则、没有使用捷径决策、没有在异常状态下强行结算。审计完成前，它只能作为工程稳定性与趋势观察工具。

### 12.4 真实牌局评测标准

只有满足以下条件的测试结果，才允许纳入“真实牌局能力评估”：

- 每局从真实洗牌、发牌、庄家、牌墙开始
- 所有电脑出牌、碰、杠、胡、过均走同一条 C# 决策链
- Godot 只负责状态调度、动画与响应，不用 GDScript 兜底替 AI 做决策
- 摸牌、出牌、碰、杠、胡、报叫、报杠、查叫、退税、归、结算均按正常状态机执行
- 没有直接写入结果、没有绕过反应链、没有跳过结算链
- 每局必须自然进入 `battle_end`、流局或正常结算
- 若出现卡死、超时熔断、强制推进、残留反应队列，该局只计入稳定性问题，不计入胜率 / 净分结论
- 报告必须记录异常原因、停在哪个阶段、当前玩家、最后一张牌、待处理反应队列

### 12.5 人机实战数据闭环

当前最可信的调参来源，是玩家与 AI 在安装包中真实打一局后的实战数据。

相关代码：

- `res://autoload/GameState.gd::_update_ai_learning_after_round`
- `res://autoload/GameState.gd::_reload_ai_learning_for_new_round`
- `res://autoload/GameState.gd::_apply_ai_learning_adjustment`
- `res://scripts/core/ai_learning_engine.gd`
- `dotnet/AI.Core/Learning/NeijiangLearningEngine.cs`

相关数据：

- `user://测试数据统计/ai学习数据.json`
- `user://测试数据统计/ai参数学习历史.json`

口径要求：

- 每局正常结束后，将本局胜负、净分、胡牌、点炮、查叫、退税、碰杠收益、AI 决策摘要累加到历史数据
- 新一局开始时，AI 读取累计学习数据，调整参数面板对应权重
- 参数变化必须可追踪，不能只在内存中悄悄改变
- 自动压测未完全审计前，不允许让压测结果直接大幅改写默认参数
- 实战局数越多，学习权重可信度越高；低局数只能小幅修正

当前结论：

- C# 烟测用于确认“单点算法能算”
- Godot 回归用于确认“规则流程没坏”
- 自动流程压测用于发现“批量卡死和趋势问题”
- 人机实战数据才是当前 AI 自进化的最高优先级输入

---

## 13. 后续维护约定

后续若出现以下改动，必须同步更新本文档：

- 阶段划分阈值变化
- 报叫/报杠判定变化
- 胡率、后验、尾盘防守阈值变化
- 碰杠收益与风险模型变化
- 番型、番数、结算文案变化
- 测试、压测、学习闭环与真实评测口径变化

本文档目标不是“讲概念”，而是保证 **文档、参数、AI 实际决策、测试口径** 四者一致。
